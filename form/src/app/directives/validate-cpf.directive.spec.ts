import { ValidateCPFDirective } from './validate-cpf.directive';

describe('ValidateCPFDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateCPFDirective();
    expect(directive).toBeTruthy();
  });
});
